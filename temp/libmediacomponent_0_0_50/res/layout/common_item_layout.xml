<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <!--item的上分割线-->
    <View
        android:id="@+id/divider_top"
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:background="@color/meida_item_divider" />

    <!--item内容部分的容器-->
    <LinearLayout
        android:id="@+id/root_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingBottom="15dp"
        android:paddingLeft="20dp"
        android:paddingRight="20dp"
        android:paddingTop="15dp">

        <!--item左边的Icon-->
        <ImageView
            android:id="@+id/left_icon"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginRight="10dp"/>

        <!--item中间部分的文字-->
        <TextView
            android:id="@+id/title_content_txv"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="14sp" />

        <!--item 中间部分的输入框（有则显示五则隐藏，默认隐藏）-->

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">
            <Switch
                android:id="@+id/right_switch"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@color/transparent"
                android:visibility="gone"/>
            <!--item右边的文字-->
            <TextView
                android:id="@+id/right_text_txv"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="14sp"
                android:layout_gravity="center"
                android:gravity="center"
                android:background="@color/transparent"
                android:textColor="@color/secondary_text"
                android:text="000"/>

            <!--item右边的Icon 默认是向右的箭头-->
            <ImageView
                android:id="@+id/right_icon_imv"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:paddingLeft="10dp"
                android:layout_gravity="center"
                android:src="@drawable/meida_item_right_arrow" />
        </LinearLayout>

    </LinearLayout>

    <!--item的下分割线-->
    <View
        android:id="@+id/divider_bottom"
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:background="@color/meida_item_divider" />
</LinearLayout>